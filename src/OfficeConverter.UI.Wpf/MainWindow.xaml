<Window x:Class="OfficeConverter.UI.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OfficeConverter.UI.Wpf"
        mc:Ignorable="d"
        Title="Office Converter" Height="450" Width="800"
        AllowDrop="True"
        Drop="Window_Drop"
        DragOver="Window_DragOver">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Content="Add Files" Margin="4" Padding="8,2" Click="AddFiles_Click"/>
            <Button Content="Add Folder" Margin="4" Padding="8,2" Click="AddFolder_Click"/>
            <Separator/>
            <Button Content="Settings" Margin="4" Padding="8,2" Click="Settings_Click"/>
        </ToolBar>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Margin="8" Padding="12" BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Background="#FFF9F9F9">
                <TextBlock Text="Drag and drop files here" HorizontalAlignment="Center"/>
            </Border>

            <ListView Grid.Row="1" ItemsSource="{Binding Jobs}" Margin="8">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="File" Width="300" DisplayMemberBinding="{Binding FileName}"/>
                        <GridViewColumn Header="Status" Width="100" DisplayMemberBinding="{Binding Status}"/>
                        <GridViewColumn Header="Progress" Width="80" DisplayMemberBinding="{Binding Progress}"/>
                        <GridViewColumn Header="Output" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding OutputPath}" TextDecorations="Underline" Foreground="Blue" Cursor="Hand" MouseDown="Output_MouseDown"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Log" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding LogPath}" TextDecorations="Underline" Foreground="Blue" Cursor="Hand" MouseDown="Log_MouseDown"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
</Window>
